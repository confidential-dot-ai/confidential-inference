# Build-time drop-in the real c8s sync hook renders (see
# control-plane-state-disk.sh, CRED_PLATFORM_DROPIN); not part of any git
# tree, stubbed here with the platform builder-lock.json pins.
[Service]
Environment=CRED_PLATFORM=tdx
