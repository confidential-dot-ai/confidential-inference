# Candidate V7 snapshot

This directory records the historical `candidate-v7` release input.
It does not define the current release process or an allowed tag format.

New release candidates use semantic tags such as `v0.13.28-rc.1`.
See `releases/README.md` for the current process.
